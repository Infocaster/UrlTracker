mode: ContinuousDeployment
branches:
  feature:
    label: alpha
    regex: ^v17/feature/
    source-branches: [ 'develop' ]
  develop:
    label: beta
    regex: ^v17/develop$
  master:
    label: ''
    regex: ^v17/main$
  hotfix:
    label: 'beta'
    regex: ^v17/hotfix
    source-branches: [ 'master' ]
  release:
    label: 'rc'
    regex: ^v17/release
ignore:
  sha: []